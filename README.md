# fitness-tracker
Frontend-only version of a future full-stack fitness tracking system.
# 🏋️ Fitness Tracker Dashboard

A simple web app built with React to log your daily steps and calories.

## 🚀 Features
- Add daily step and calorie data
- View entries in a table
- Data saved in browser storage

## 🛠️ Tech Stack
- React.js
- HTML, CSS, JavaScript

## 📦 Setup
1. Clone the repo
2. Run `npm install`
3. Run `npm start`

